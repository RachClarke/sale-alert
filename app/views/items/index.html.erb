<h2>Your Items</h2>

<ul>
  <% @items.each do |item| %>
    <li><%= link_to item.name, item_path(item) %> | <%= link_to 'Edit', edit_item_path(item) %> | <%= link_to 'Delete', item_path(item), method: :delete, data: {confirm: "Are you sure?"} %></li>
  <% end %>
</ul>

<%= link_to 'Track a new item', new_item_path %>
